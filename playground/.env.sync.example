# Local Mac sync agent (not used by cloud API container)
SCREENPIPE_SYNC_ENABLED=1
SYNC_SECRET=change-me

# ScreenPipe paths on capture machine
SCREENPIPE_DB=/Volumes/Kasliwal v2/screenpipe-data/db.sqlite
DATABASE_URL=<same neon as production>
