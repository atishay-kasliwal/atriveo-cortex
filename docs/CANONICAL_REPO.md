# Canonical repository moved

**All platform implementation lives in [`atriveo-capture-agent`](https://github.com/atishay-kasliwal/atriveo-capture-agent).**

This repository (`atriveo-cortex`) is **reference only** — production Mac mini sync and historical code to port from.

| What | Where |
|------|-------|
| Engineering handbook | `atriveo-capture-agent/docs/platform/` |
| ADRs | `atriveo-capture-agent/docs/adr/` |
| Implementation roadmap | `atriveo-capture-agent/docs/IMPLEMENTATION_ROADMAP.md` |
| New features | **Do not add here** |

Clone the capture agent repo:

```bash
git clone https://github.com/atishay-kasliwal/atriveo-capture-agent.git
```

Mac mini production is unchanged until Milestone 13 cutover documented in the capture-agent roadmap.
